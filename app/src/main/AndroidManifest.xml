<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".BillBroApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/avatar1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/avatar1"
        android:supportsRtl="true"
        android:theme="@style/Theme.BillBro">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".screens.activity.LoginActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".screens.activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".screens.activity.GroupActivity"
            android:exported="false" />

        <activity
            android:name=".screens.activity.NotificationActivity"
            android:exported="false"/>

        <activity
            android:name=".screens.activity.MainActivity"
            android:exported="false"/>

    </application>

</manifest>